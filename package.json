{
	"scripts": {
		"dashboard:css:build": "sass -s compressed --no-source-map src/Menu/assets",
		"dashboard:css:watch": "sass -s compressed --no-source-map -w src/Menu/assets"
	},
	"devDependencies": {
		"sass": "^1.80.1"
	}
}