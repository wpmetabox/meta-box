{
	"scripts": {
		"dashboard:css:build": "sass -s compressed --no-source-map src/Dashboard/assets",
		"dashboard:css:watch": "sass -s compressed --no-source-map -w src/Dashboard/assets"
	},
	"devDependencies": {
		"sass": "^1.80.1"
	}
}